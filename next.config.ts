import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['img.mlbstatic.com', 'www.mlbstatic.com'], // Add any other domains you're using too
  },
};

export default nextConfig;
